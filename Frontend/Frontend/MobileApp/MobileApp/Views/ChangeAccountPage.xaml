<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.ChangeAccountPage"
             Style="{StaticResource DefaultPageStyle}">
    <ContentPage.Content>
        <StackLayout Spacing="20" VerticalOptions="FillAndExpand">
            <Label Text="Change account information" Style="{StaticResource LabelH1}"/>
            <StackLayout VerticalOptions="StartAndExpand" Padding="15,0">
                <Label Text="First name: " Style="{StaticResource LabelH2}"/>
                <Entry x:Name="EntryFirstName" Text="{Binding account.firstName}" FontSize="Medium"/>
                <Label Text="Last name: " Style="{StaticResource LabelH2}"/>
                <Entry x:Name="EntryLastName" Text="{Binding account.lastName}" FontSize="Medium"/>
                <Label Text="Email: " Style="{StaticResource LabelH2}"/>
                <Entry x:Name="EntryEmail" Text="{Binding account.email}" FontSize="Medium"/>
                <Label Text="Username: " Style="{StaticResource LabelH2}"/>
                <Entry x:Name="EntryUsername" Text="{Binding account.username}" FontSize="Medium"/>
                <Label Text="Date of birth: " Style="{StaticResource LabelH2}"/>
                <DatePicker x:Name="DatePickerDateOfBirth" Date="{Binding account.dateOfBirth}" FontSize="Medium"/>
            </StackLayout>
            <StackLayout VerticalOptions="End" Padding="15">
                <Button Text="Update account" x:Name="ButtonUpdate" Clicked="OnButtonUpdateClicked" Style="{StaticResource ButtonStyle}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>