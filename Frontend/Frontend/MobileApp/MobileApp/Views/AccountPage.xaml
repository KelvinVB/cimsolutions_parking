<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:MobileApp.ViewModels;assembly=MobileApp"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.AccountPage">
    <ContentPage.BindingContext>
        <viewModel:AccountViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
      <StackLayout>
            <Label Text="Account" FontSize="Large"/>
            <Label Text="Name: " FontSize="Medium"/>
            <Label Text="{Binding account.firstName}" FontSize="Medium"/>
            <Label Text="{Binding account.firstName}" FontSize="Medium"/>
            <Button Text="Change account information" x:Name="ButtonUpdate" FontSize="Medium" Clicked="OnButtonUpdateClicked"/>
            <Button Text="Log out" x:Name="ButtonLogOut" FontSize="Medium" Clicked="OnButtonLogOutClicked"/>
            <Button Text="Delete account" x:Name="ButtonDelete" FontSize="Medium" Clicked="OnButtonDeleteClicked"/>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>