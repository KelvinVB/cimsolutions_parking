<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:MobileApp.ViewModels;assembly=MobileApp"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.AccountPage">
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Account" Style="{StaticResource LabelH1}"/>
            <Label Text="Name: " Style="{StaticResource LabelH2}"/>
            <Label Text="{Binding account.firstName}" Style="{StaticResource LabelH2}"/>
            <Label Text="{Binding account.lastName}" Style="{StaticResource LabelH2}"/>
            <Button Text="Change account information" x:Name="ButtonUpdate" FontSize="Medium" Clicked="OnButtonUpdateClicked" Style="{StaticResource ButtonStyle}"/>
            <Button Text="Log out" x:Name="ButtonLogOut" FontSize="Medium" Clicked="OnButtonLogOutClicked" Style="{StaticResource ButtonStyle}"/>
            <Button Text="Delete account" x:Name="ButtonDelete" FontSize="Medium" Clicked="OnButtonDeleteClicked" Style="{StaticResource ButtonStyle}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>